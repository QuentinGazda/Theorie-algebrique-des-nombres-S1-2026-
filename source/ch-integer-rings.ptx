<?xml version="1.0" encoding="UTF-8"?>

<chapter xml:id="ch-integer-rings" xmlns:xi="http://www.w3.org/2001/XInclude">
  <title>Anneaux d'entiers</title>


<section xml:id="sec-OK">
  <title>Structure de <m>\mathcal{O}_K</m></title>
  
<p>
  Soit <m>K</m> un corps de nombres de degré <m>n=[K:\mathbb{Q}]</m>. On définit l'<em>anneau des entiers</em> de <m>K</m> comme l'ensemble
</p>
<md>
  \mathcal{O}_K = K\cap \overline{\mathbb{Z}}.
</md>

<p>
  L'anneau <m>\mathcal{O}_K</m> est en général difficile à déterminer. Par exemple, si l'on a montré que <m>K</m> est engendré par un seul élément par le théorème de l'élément primitif (<xref ref="thm-element-primitif"/>), il n'est pas toujours vrai que <m>\mathcal{O}_K</m> est monogène, c-à-d de la forme <m>\mathbb{Z}[\alpha]</m> pour un entier algébrique <m>\alpha</m>.
</p>

<example>
  <title>
  Non-monogénie d'anneaux d'entiers dans des extensions biquadratiques
  </title>
  <p>
    Si <m>m,n</m> sont des entiers sans facteur carré distincts congrus à <m>1 \pmod{8}</m>, alors on peut montrer que l'anneau des entiers de <m>K=\mathbb{Q}(\sqrt{m},\sqrt{n})</m> est donné par <m>\mathcal{O}_K=\mathbb{Z}\left[\frac{1+\sqrt{m}}{2},\frac{1+\sqrt{n}}{2}\right]</m> et qu'il n'est pas monogène.
  </p>
</example>

  <p>
    Si <m>K</m> est un corps de degré deux, on a déjà vu que <m>\mathcal{O}_K</m> est libre de rang <m>[K:\mathbb{Q}]=2</m> comme groupe abélien (<xref ref="cor-entiers-quadratiques-base"/>). Le but de cette section est de généraliser ce résultat à tout corps de nombres <m>K</m> en utilisant le plongement canonique de <m>K</m> (<xref ref="def-plongement-canonique"/>).
  </p>  
  <p>
    On désigne par <m>\Sigma(K)=\mathrm{Hom}(K,\mathbb{C})</m> l'ensemble des plongements de <m>K</m>.
  </p>

  <definition xml:id="def-plongements-reels-et-complexes">
    <title> Plongements réels et complexes</title>
    <statement>
      <p>
        Un <em>plongement réel</em> de <m>K</m> est un plongement <m>\sigma\in \Sigma(K)</m> dont l'image <m>\sigma(K)</m> est contenue dans <m>\mathbb{R}</m>.
      </p>
      <p>  
        Un <em>plongement complexe</em> de <m>K</m> est un plongement de <m>K</m> qui n'est pas réel.
      </p>
    </statement>
  </definition>
  <p>
    Étant donné un plongement <m>\sigma:K\to \mathbb{C}</m>, on peut définir <em> son plongement conjugué</em>
  </p>
  <md> \overline{\sigma}:K\longrightarrow \mathbb{C}, \quad x\longmapsto \overline{\sigma(x)}.</md>
  <p>
  Alors <m>\overline{\sigma}</m> est aussi un plongement de <m>K</m> et l'opération <m>\sigma\mapsto \overline{\sigma}</m> définit une involution de <m>\Sigma(K)</m>. Les points fixes de cette involution sont précisément les plongements réels, tandis que les plongements complexes se regroupent par paires <m>(\sigma,\overline{\sigma})</m>. De cette manière, on peut écrire 
  </p>
  <md>
  \Sigma(K)=\Sigma_1 \sqcup \Sigma_2 \sqcup \overline{\Sigma}_2
  </md>
  <p>
  où <m>\Sigma_1</m> est l'ensemble des plongements réels et <m>\Sigma_2</m> est un ensemble de représentants des paires de plongements complexes non réels deux-à-deux non conjugués.
  </p>  
  <p>
   En notant <m>r_1</m> le nombre de plongements réels de <m>K</m> et <m>r_2</m> le nombre de plongements complexes non deux-à-deux conjugués, on obtient la relation
</p>
<me>[K:\mathbb{Q}]=r_1+2r_2.</me>

<example>
  <title> Corps quadratiques réels et imaginaires </title>
  <p> 
  Soit <m>d</m> un entier sans facteur carré et <m>K=\mathbb{Q}(\sqrt{d})</m>. Alors:
  </p>
    <ul>
      <li>
        <p>
         Si <m>d&gt;0</m>, alors <m>K</m> est appelé un <em>corps quadratique réel</em> et possède deux plongements réels: l'inclusion canonique <m>\mathrm{id}_K</m> et le morphisme <m>\sigma: a+b\sqrt{d}\mapsto a-b\sqrt{d}</m>. Si <m>d&lt;0</m>, alors <m>K</m> est appelé un <em>corps quadratique imaginaire</em> et ne possède pas de plongement réel: les deux plongements complexes <m>\mathrm{id}_K</m> et <m>\sigma: a+b\sqrt{d}\mapsto a-b\sqrt{d}</m> sont conjugués l'un de l'autre.
        </p>
      </li>
      <li>
        <p>
          Si <m>d&lt;0</m>, alors <m>K</m> est appelé un <em>corps quadratique imaginaire</em> et ne possède pas de plongement réel: les deux plongements complexes <m>\mathrm{id}_K</m> et <m>\sigma: a+b\sqrt{d}\mapsto a-b\sqrt{d}</m> sont conjugués l'un de l'autre.
        </p>
      </li>
    </ul>  
</example>

<example>
  <p>
    Soit <m>K=\mathbb{Q}(\sqrt[3]{2})</m>. Le polynôme minimal de <m>\sqrt[3]{2}</m> est <m>X^3-2</m> qui possède comme racines complexes <m>\sqrt[3]{2}</m>, <m>\sqrt[3]{2}\omega</m>, et <m>\sqrt[3]{2}\omega^2</m> où <m>\omega=e^{2i\pi/3}</m> est une racine cubique primitive de l'unité. <m>K</m> est un corps de nombres de degré <m>3</m> qui possède un seul plongement réel, l'inclusion canonique, et un seul plongement complexe non réel, défini par <m>\sigma:\sqrt[3]{2}\mapsto \sqrt[3]{2}\omega</m>. On a donc <m>r_1=1</m> et <m>r_2=1</m>.
  </p>
</example>

<p>
  Numérotons les plongements de <m>K</m> de la manière suivante:
</p>
  <md>
    <mrow> \Sigma(K) \amp =\{\sigma_1,\ldots,\sigma_n\},</mrow>
    <mrow> \Sigma_1 \amp = \{\sigma_1,\ldots,\sigma_{r_1}\},</mrow>
    <mrow> \Sigma_2 \amp = \{\sigma_{r_1+1},\ldots,\sigma_{r_1+r_2}\},</mrow>
    <mrow> \overline{\Sigma}_2 \amp = \{\sigma_{r_1+r_2+1},\ldots,\sigma_{n}\}.</mrow>
  </md>

<definition xml:id="def-plongement-canonique">
  <title>Plongement canonique</title>
  <statement>
    <p>
      On définit alors le <em>plongement canonique</em> de <m>K</m> comme le morphisme de <m>\mathbb{Q}</m>-espaces vectoriels
    </p>
  <md>
  <mrow>\sigma: K \amp \to \mathbb{R}^{r_1}\times \mathbb{C}^{r_2}  </mrow>
  <mrow>x \amp \mapsto (\sigma_1(x),\ldots,\sigma_{r_1}(x),\sigma_{r_1+1}(x),\ldots,\sigma_{r_1+r_2}(x))</mrow>
  </md>
  </statement>
</definition>

<p>
  Le plongement canonique est application injective de <m>K</m> dans <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m> (car chaque <m>\sigma_i</m> indépendament, en tant que morphisme de corps, est injectif).
</p>

<p>
  Le produit <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m> est un espace vectoriel réel de dimension <m>n=r_1+2r_2</m>. On l'identifiera à <m>\mathbb{R}^n</m> en utilisant la base canonique de <m>\mathbb{R}^{r_1}</m> et la base <m>(1,i)</m> de <m>\mathbb{C}</m>; on appelle cette famille <em>la base canonique de <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m></em>. On est donc dans la cadre d'application de la <xref ref="sec-reseaux"/>.
</p>

<theorem xml:id="thm-reseau-du-plongement-canonique">
  <statement>
    <p>
      L'image de <m>\mathcal{O}_K</m> par le plongement canonique est un réseau de <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m>.
    </p>
  </statement>
</theorem>
<proof>
  <p>
    On commence par énoncer deux lemmes qui, ensembles, garantissent que <m>\sigma(\mathcal{O}_K)</m> contient une base de <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m>.
  </p>
   <lemma xml:id="lem-covolume-dune-base-dun-corps-de-nombres">
    <statement>
      <p>
        Soit <m>(e_1,\ldots,e_n)</m> une base de <m>K</m> en tant que <m>\mathbb{Q}</m>-espace vectoriel. Alors <m>(\sigma(e_1),\ldots,\sigma(e_n))</m> est une <m>\mathbb{R}</m>-base de <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m> qui engendre un réseau de covolume <m>2^{-r_2}\sqrt{|\mathrm{disc}(e_1,\ldots,e_n)|}</m>.
      </p>
    </statement>
  </lemma>
  <proof>
    <p>
      On rappelle que le covolume de la famille <m>(\sigma(e_1),\ldots,\sigma(e_n))</m> dans l'espace <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m> est donné par la valeur absolue du déterminant de la matrice de changement de base entre <m>(\sigma(e_1),\ldots,\sigma(e_n))</m> et la base canonique de <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m> (<em>cf.</em> <xref ref="def-covolume"/>). Pour <m>i\in \{1,\ldots,n\}</m>, la <m>i</m>ième coordonnée <m>\sigma(e_i)</m> s'écrit dans la base canonique comme 
    </p>
    <md>
      (\sigma_1(e_i),\ldots,\sigma_{r_1}(e_i),\mathrm{Re}~\sigma_{r_1+1}(e_i),\mathrm{Im}~\sigma_{r_1+1}(e_i),\ldots,\mathrm{Re}~\sigma_{r_1+r_2}(e_i),\mathrm{Im}~\sigma_{r_1+r_2}(e_i)).
    </md>
    <p>
      Cela montre que le covolume est donné par la valeur absolue du déterminant de la matrice
    </p>
    <md>
      \begin{pmatrix}
      \sigma_1(e_1) \amp \cdots \amp \sigma_{r_1}(e_1) \amp \mathrm{Re}~\sigma_{r_1+1}(e_1) \amp \mathrm{Im}~\sigma_{r_1+1}(e_1) \amp \cdots \amp \mathrm{Re}~\sigma_{r_1+r_2}(e_1) \amp \mathrm{Im}~\sigma_{r_1+r_2}(e_1) \\
      \sigma_1(e_2) \amp \cdots \amp \sigma_{r_1}(e_2) \amp \mathrm{Re}~\sigma_{r_1+1}(e_2) \amp \mathrm{Im}~\sigma_{r_1+1}(e_2) \amp \cdots \amp \mathrm{Re}~\sigma_{r_1+r_2}(e_2) \amp \mathrm{Im}~\sigma_{r_1+r_2}(e_2) \\
      \vdots \amp \ddots \amp \vdots \amp \vdots \amp \vdots \amp \ddots \amp \vdots \amp \vdots \\
      \sigma_1(e_n) \amp \cdots \amp 	\sigma_{r_1}(e_n) 	\amp 	\mathrm{Re}~\sigma_{r_1+1}(e_n) 	\amp 	\mathrm{Im}~\sigma_{r_1+1}(e_n) 	\amp 	\cdots 	\amp 	\mathrm{Re}~\sigma_{r_1+r_2}(e_n) 	\amp 	\mathrm{Im}~\sigma_{r_1+r_2}(e_n)
      \end{pmatrix}.
    </md>
    <p>
    En multipliant à droite cette matrice par la matrice complexe de taille <m>n=r_1+2r_2</m> donnée par
    </p>
    <men xml:id="matrice-de-changement-complexe">
      \left(
        \begin{array}{c|cccc}
        I_{r_1} \amp 0      \amp 0      \amp \cdots \amp 0 \\
        \hline
        0 \amp \begin{pmatrix} 1 \amp i \\[2pt] 1 \amp -i \end{pmatrix} \amp 0 \amp \cdots \amp 0 \\
        0 \amp 0 \amp \begin{pmatrix} 1 \amp i \\[2pt] 1 \amp -i \end{pmatrix} \amp \ddots \amp \vdots \\
        \vdots \amp \vdots \amp \ddots \amp \ddots \amp 0 \\
        0 \amp 0 \amp \cdots \amp 0 \amp \begin{pmatrix} 1 \amp i \\[2pt] 1 \amp -i \end{pmatrix}
        \end{array}
      \right)
    </men>
    <p>
    on trouve la matrice 
    </p>
    <md>
      \begin{pmatrix}
      \sigma_1(e_1) \amp \cdots \amp \sigma_{r_1}(e_1) \amp \sigma_{r_1+1}(e_1) \amp \overline{\sigma_{r_1+1}}(e_1) \amp \cdots \amp \overline{\sigma_{r_1+r_2}}(e_1) \\
      \sigma_1(e_2) \amp \cdots \amp \sigma_{r_1}(e_2) \amp \sigma_{r_1+1}(e_2) \amp \overline{\sigma_{r_1+1}}(e_2) \amp \cdots \amp \overline{\sigma_{r_1+r_2}}(e_2) \\
      \vdots \amp \ddots \amp \vdots \amp \vdots \amp \vdots \amp \ddots \amp \vdots \\
      \sigma_1(e_n) \amp \cdots \amp \sigma_{r_1}(e_n) \amp \sigma_{r_1+1}(e_n) \amp \overline{\sigma_{r_1+1}}(e_n) \amp \cdots \amp \overline{\sigma_{r_1+r_2}}(e_n) \\
      \end{pmatrix}
    </md>
    <p>
    qui n'est autre que la matrice <m>(\sigma_i(e_j))_{i,j}</m> dont le carré du déterminant est égal à <m>\mathrm{disc}(e_1,\ldots,e_n)</m>. Le déterminant de la matrice réelle initiale est donc égal à une racine du discriminant divisé par <m>(2i)^{r_2}</m>, le déterminant de la matrice complexe <xref ref="matrice-de-changement-complexe"/>. La valeur absolue, et donc le covolume, s'en déduit.
    </p>
  </proof>

  <lemma xml:id="lem-OK-contient-une-base-de-K">
    <statement>
      <p>
        <m>\mathcal{O}_K</m> contient une base de <m>K</m> en tant que <m>\mathbb{Q}</m>-espace vectoriel.
      </p>
    </statement>
  </lemma>
  <proof>
    <p>
      Soit <m>(e_1,\ldots,e_n)</m> une base de <m>K</m> en tant que <m>\mathbb{Q}</m>-espace vectoriel. Pour chaque <m>i\in\{1,\ldots,n\}</m>, il existe un entier non nul <m>d_i</m> tel que <m>d_i e_i\in \overline{\mathbb{Z}}</m> (<xref ref="lem-denominateur"/>). Ainsi <m>(d_1e_1,\ldots,d_ne_n)</m> est encore une base de <m>K</m> comme <m>\mathbb{Q}</m>-espace vectoriel et est contenue dans <m>\mathcal{O}_K=K\cap \overline{\mathbb{Z}}</m>.
    </p>
  </proof>

  <p>
    D'après le <xref ref="lem-OK-contient-une-base-de-K"/>, il reste à montrer que l'image <m>\sigma(\mathcal{O}_K)</m> est discrète dans <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m>. On équippe alors <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m> de la norme sup <m>\|\cdot\|_{\infty}</m> dans la base canonique.
  </p>
  <p>
    Pour <m>r>0</m> et <m>x\in \mathcal{O}_K</m>, l'inégalité <m>\|x\|_{\infty}\leq r</m> signifie que <m>|\sigma_i(x)|\leq r</m> pour <m>i\in \{1,\ldots,r_1\}</m> et que <m>|\mathrm{Re}~\sigma_{r_1+j}(x)|\leq r</m> et <m>|\mathrm{Im}~\sigma_{r_1+j}(x)|\leq r</m> pour <m>j\in \{1,\ldots,r_2\}</m>. En particulier, <m>|\sigma_i(x)|\leq \sqrt{2} r</m> pour tout <m>i\in \{1,\ldots,n\}</m>.  
  </p>
  <p>
    De l'identité
  </p>
    <md>
      \chi_{x,K}(X)=\prod_{i=1}^n (X-\sigma_i(x))
    </md>
  <p>
    (<xref ref="prop-formule-traces-normes"/>), on déduit que le <m>m</m>-ième coefficient <m>a_m</m> de <m>\chi_{x,K}(X)</m> vérifie
  </p>
  <md>
    |a_m|=\left|(-1)^m \sum_{1\leq i_1 &lt; \cdots &lt; i_m\leq n}{\sigma_{i_1}(x)\cdots \sigma_{i_m}(x)}\right|\leq \binom{n}{m}\sqrt{2}^m r^m
  </md>
  <p>
    Puisqu'il n'y a qu'un nombre fini de polynômes à coefficients entiers vérifiant ces inégalités, il n'y a qu'un nombre fini de <m>\chi_{x,K}(X)</m> possibles, et donc qu'un nombre fini de tels <m>x</m> (racine de <m>\chi_{x,K}(X)</m>). Ainsi, <m>\sigma(\mathcal{O}_K)</m> est discret dans <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m>.
  </p>
</proof>

<example>
  <title>Plongement canonique des corps quadratiques</title>
  <p>
    Soit <m>K=\mathbb{Q}(\sqrt{5})</m>. Le corps <m>K</m> est quadratique réel, c-à-d que <m>(r_1,r_2)=(2,0)</m>. Le plongement canonique s'écrit donc
  </p>
  <md>
    <mrow> \sigma: \amp K\longrightarrow \mathbb{R}^2</mrow>
    <mrow> \amp 1 \longmapsto (1,1)</mrow>
    <mrow> \amp \sqrt{5} \longmapsto (\sqrt{5},-\sqrt{5})</mrow>
  </md>
  <p>
    Comme <m>\mathcal{O}_K=\mathbb{Z}[\varphi]</m> où <m>\varphi=\frac{1+\sqrt{5}}{2}</m>, le réseau <m>\sigma(\mathcal{O}_K)</m> est engendré par les vecteurs <m>(1,1)</m> et <m>\left(\varphi,\overline{\varphi}\right)</m> où <m>\overline{\varphi}=\frac{1-\sqrt{5}}{2}</m> est le conjugué de <m>\varphi</m>.
  </p>
<p>
  Son covolume est <m>\det\begin{pmatrix} 1 \amp \varphi \\ 1 \amp \overline{\varphi} \end{pmatrix}^2 = 5</m>.
</p>
</example>

<p>
On déduit de la <xref ref="prop-base-reseau"/> le corollaire suivant :
</p>
<corollary xml:id="cor-structure-of-OK">
  <statement>
    <p>
      L'anneau <m>\mathcal{O}_K</m> est un <m>\mathbb{Z}</m>-module libre de rang <m>n</m>; autrement dit, il existe une base <m>(e_1,\ldots,e_n)</m> de <m>K</m> en tant que <m>\mathbb{Q}</m>-espace vectoriel telle que <m>\mathcal{O}_K=\bigoplus_{i=1}^n \mathbb{Z}e_i</m>.
    </p>
  </statement>
</corollary>

</section>


<section xml:id="sec-discriminant-de-K">
  <title>Discriminant de <m>K</m></title>
<p>
Soit <m>K</m> un corps de nombres de degré <m>n=[K:\mathbb{Q}]</m> et <m>\mathcal{O}_K</m> son anneau des entiers. D'après le <xref ref="cor-structure-of-OK"/>, il existe une <m>\mathbb{Z}</m>-base <m>(e_1,\ldots,e_n)</m> de <m>\mathcal{O}_K</m>. Un choix alternatif de base diffère de la première par une matrice de <m>\mathrm{GL}_n(\mathbb{Z})</m>, soit déterminant <m>\pm 1</m>. Par conséquent, le discriminant <m>\mathrm{disc}(e_1,\ldots,e_n)</m> ne dépend pas du choix de la base de <m>\mathcal{O}_K</m> (<xref ref="prop-discriminants"/>, <xref ref="item-discriminant-changement-base"/>). On peut donc définir le discriminant de <m>K</m> comme suit.
</p>

  <definition xml:id="def-discriminant-dun-corps-de-nombres">
    <title>Discriminant d'un corps de nombres</title>
    <statement>
      <p>
        Le <em>discriminant</em> de <m>K</m> est le discriminant d'une base de <m>\mathcal{O}_K</m> en tant que <m>\mathbb{Z}</m>-module. On le note <m>\mathrm{disc}(K)</m>, <m>\mathrm{disc}(\mathcal{O}_K)</m> ou même <m>\Delta_K</m>.
      </p>
    </statement>
  </definition>

  <example>
    <title> Discriminant des corps quadratiques </title>
    <p>
      Soit <m>K=\mathbb{Q}(\sqrt{d})</m> un corps quadratique où <m>d</m> est un entier sans facteur carré. On a vu que l'anneau des entiers de <m>K</m> est donné par
    </p>
    <md>
      \mathcal{O}_K = \begin{cases}
      \mathbb{Z}[\sqrt{d}] \amp \text{si } d\equiv 2,3 \pmod{4}, \\
      \mathbb{Z}\left[\frac{1+\sqrt{d}}{2}\right] \amp \text{si } d\equiv 1 \pmod{4}.
      \end{cases}
    </md>
    <p>
      Le discriminant de <m>K</m> se calcule alors comme <m>\det\left(\begin{smallmatrix} 1 \amp \sqrt{d} \\ 1 \amp -\sqrt{d} \end{smallmatrix}\right)^2</m> si <m>d\equiv 2,3 \pmod{4}</m>, et comme <m>\det\left(\begin{smallmatrix} 1 \amp \frac{1+\sqrt{d}}{2} \\ 1 \amp \frac{1-\sqrt{d}}{2} \end{smallmatrix}\right)^2</m> si <m>d\equiv 1 \pmod{4}</m>. On trouve ainsi
    </p>
    <md>
      \mathrm{disc}(\mathcal{O}_K) = \begin{cases}
      4d \amp \text{si } d\equiv 2,3 \pmod{4}, \\
      d \amp \text{si } d\equiv 1 \pmod{4}.
      \end{cases}
    </md>
  </example>

  <p>
    Le plongement canonique <m>\sigma:K\to \mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m> permet d'identifier <m>\mathcal{O}_K</m> à un réseau de l'espace vectoriel réel <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m> (<xref ref="thm-reseau-du-plongement-canonique"/>). On calcule son covolume à l'aide de la proposition suivante, qui découle immédiatement du <xref ref="lem-covolume-dune-base-dun-corps-de-nombres"/>.
  </p>
  <proposition xml:id="prop-covolume-sigmaOK">
    <statement>
      <p>
        Le réseau <m>\sigma(\mathcal{O}_K)</m> est de covolume <m>2^{-r_2}\sqrt{|\mathrm{disc}(\mathcal{O}_K)|}</m>.
      </p>
    </statement>
  </proposition>

  <remark>
    <p>
      Soit <m>D</m> le sous-ensemble de <m>\mathbb{Z}</m> formé des <m>|\mathrm{disc}(f_1,\ldots,f_n)|</m> où <m>(f_1,\ldots,f_n)</m> est une famille de <m>\mathcal{O}_K</m>. Alors <m>D</m> est non réduit à zéro et admet un plus petit élément qui est précisément <m>|\mathrm{disc}(\mathcal{O}_K)|</m> (on le déduit de la <xref ref="prop-discriminants"/>, <xref ref="item-discriminant-changement-base"/>).
   </p>
  </remark>

<p>
  Plus généralement, la <xref ref="def-discriminant-dun-corps-de-nombres"/> s'étend à un sous-groupe de <m>\mathcal{O}_K</m> contenant une base de <m>K</m>.
</p>
  <definition xml:id="def-discriminant-dun-sous-groupe">
    <title>Discriminant d'un sous-groupe de <m>\mathcal{O}_K</m></title>
    <statement>
      <p>
        Soit <m>A\subset \mathcal{O}_K</m> un sous-groupe contenant une base de <m>K</m><fn>La même preuve que celle du <xref ref="thm-reseau-du-plongement-canonique"/> montre que <m>\sigma(A)</m> est un réseau de <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m>. En particulier, on notera que <m>A</m> est libre de rang <m>n</m> en tant que <m>\mathbb{Z}</m>-module.</fn>. Le <em>discriminant</em> de <m>A</m> est le discriminant d'une base de <m>A</m> en tant que <m>\mathbb{Z}</m>-module. On le note <m>\mathrm{disc}(A)</m>.
      </p>
    </statement>
  </definition>

<p>
  <xref ref="def-discriminant-dun-sous-groupe"/> s'applique en particulier aux anneaux de la forme <m>\mathbb{Z}[\alpha]</m> où <m>\alpha</m> est un élément primitif de <m>K</m>, ou bien aux idéaux non-nuls<fn>En effet, <m>I</m> est un idéal non nul, si <m>x\in I\setminus\{0\}</m> et si <m>(e_1,\ldots,e_n)</m> désigne une <m>\mathbb{Z}</m>-base de <m>\mathcal{O}_K</m>, alors <m>(xe_1,\ldots,xe_n)</m> est une base de <m>K</m> contenue dans <m>I</m>.</fn>.
</p>

<proposition xml:id="prop-discriminant-indice">
  <statement>
    <p>
      Soit <m> A\subset B</m> des sous-groupes de <m>\mathcal{O}_K</m> contenant une base de <m>K</m>. Alors <m>A</m> est d'indice fini dans <m>B</m> et on a la relation
    </p>
    <md>
      |\mathrm{disc}(A)|=|B/A|^2\cdot |\mathrm{disc}(B)|.
    </md>
  </statement>
</proposition>
<proof>
  <p>
    D'après la <xref ref="prop-comparaison-covolumes"/> et le <xref ref="lem-covolume-dune-base-dun-corps-de-nombres"/>, on a 
  </p>
  <md>
    |B/A|=|\sigma(B)/\sigma(A)|=\frac{\mathrm{covol}(\sigma(A))}{\mathrm{covol}(\sigma(B))}=\sqrt{\frac{\mathrm{disc}(A)}{\mathrm{disc}(B)}}.
  </md>
</proof>

<p>
  La <xref ref="prop-discriminant-indice"/> est fondamentale pour le calcul de <m>\mathcal{O}_K</m>. Elle indique en particulier que le discriminant d'un sous-anneau <m>A\subset \mathcal{O}_K</m> ne peut diminuer que d'un carré ! Cela implique par exemple que si <m>\mathrm{disc}(A)</m> est un entier sans facteur carré, alors <m>A=\mathcal{O}_K</m>.
</p>

<example>
  <title> Nombre plastique </title>
  <p>
  Soit <m>\psi</m> le <em>nombre plastique</em> :
  </p>
<md>
  \psi=\sqrt[3]{\frac12+\frac{\sqrt{69}}{18}}+\sqrt[3]{\frac12-\frac{\sqrt{69}}{18}}
</md>
<p>
  Ce nombre est l'unique nombre réel vérifiant <m>\psi^3=\psi+1</m> (de manière parallèle au <em>nombre d'or</em> qui vérifie <m>\varphi^2=\varphi+1</m>). C'est un entier algébrique de degré trois.
</p>
<p>
  Utilisons la <xref ref="prop-discriminant-indice"/> pour déterminer l'anneau des entiers du corps de nombres <m>K=\mathbb{Q}(\psi)</m>. On a <m>\mathbb{Z}[\psi]\subset \mathcal{O}_K</m> et, pour montrer que cette inclusion est une égalité, il suffit de montrer que <m>|\mathcal{O}_K/\mathbb{Z}[\psi]|=1</m>. Or la <xref ref="prop-discriminant-indice"/> donne
</p>
<md>
  |\mathrm{disc}(\mathbb{Z}[\psi])|=|\mathrm{disc}(\mathcal{O}_K)|\cdot |\mathcal{O}_K/\mathbb{Z}[\psi]|^2.
</md>
<p>
  Comme <m>X^3-X-1</m> est le polynôme minimal de <m>\psi</m>, on a
</p>
  <md>
    \mathrm{disc}(\mathbb{Z}[\psi])=\mathrm{disc}(1,\psi,\psi^2)=\mathrm{disc}(X^3-X-1)=-23.
  </md>
<p>
    La première égalité suit de la définition, la seconde s'obtient par le <xref ref="cor-discriminant-plongements"/>, enfin on peut le calculer le discriminant d'un polynôme à l'aide des formules <xref ref="exemples-discriminants-polynomes"/>). Comme <m>23</m> est sans facteur carré, on a nécessairement <m>|\mathcal{O}_K/\mathbb{Z}[\psi]|=1</m>.
</p>
</example>
  
 
</section>


<section xml:id="sec-norme-ideaux">
  <title>Norme d'un idéal</title>
  
  <p>
  Soit <m>I</m> un idéal non nul de <m>\mathcal{O}_K</m>. Si <m>\sigma</m> désigne le plongement canonique, on a vu que <m>\sigma(I)\subset \sigma(\mathcal{O}_K)</m> est une inclusion de réseaux de <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m>. Son indice <m>|\mathcal{O}_K/I|</m> est alors fini. 
  </p>
  <definition xml:id="def-norme-ideal">
    <title>Norme d'un idéal</title>
    <statement>
      <p>
        On appelle <em>norme</em> de <m>I</m> et on note <m>N(I)</m> l'indice <m>|A/I|</m> de <m>I</m> dans <m>\mathcal{O}_K</m>.
      </p>
    </statement>
  </definition>

  <p>
    La terminologie <em>norme</em> est justifiée par la relation qu'elle entretient avec la norme d'un élément de <m>K</m>.
  </p>
  <proposition xml:id="prop-norme-ideal-element">
    <statement>
      <p>
        Soit <m>a\in \mathcal{O}_K</m> et <m>I</m> un idéal non nul. Alors <m>N(aI)=|N_{K/\mathbb{Q}}(a)| N(I)</m>. En particulier, <m>N((a))=|N_K(a)|</m>.
      </p>
    </statement>
  </proposition>
  <proof>
    <p>
      Notons que <m>\mathrm{disc}_K(aI)=N_{K}(a)^2\mathrm{disc}_K(I)</m> : en effet, si <m>(e_1,\ldots,e_n)</m> est une base de <m>I</m>, alors <m>(ae_1,\ldots,ae_n)</m> est une base de <m>aI</m> et alors 
    </p>
    <md>
      <mrow> \mathrm{disc}_K(aI) \amp =\mathrm{disc}(ae_1,\ldots,ae_n) </mrow>
      <mrow> \amp =\det\begin{pmatrix} \sigma_i(ae_j) \end{pmatrix}^2=\det\begin{pmatrix} \sigma_i(a)\sigma_i(e_j) \end{pmatrix}^2=N_{K}(a)^2\mathrm{disc}_K(I). </mrow>
    </md>
    <p>
      De plus, comme <m>\sigma(aI)</m> et <m>\sigma(I)</m> sont des sous-réseaux de <m>\sigma(\mathcal{O}_K)</m> dans <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m> de covolumes respectifs <m>2^{-r_2}\sqrt{|\mathrm{disc}_K(aI)|}</m> et <m>2^{-r_2}\sqrt{|\mathrm{disc}_K(I)|}</m> d'après le <xref ref="lem-covolume-dune-base-dun-corps-de-nombres"/>, on a par la <xref ref="prop-comparaison-covolumes"/> que
    </p>
    <md>
      <mrow> \sqrt{|\mathrm{disc}_K(\mathcal{O}_K)|} \amp = |\mathcal{O}_K/aI|  \sqrt{|\mathrm{disc}_K(aI)|} </mrow>
      <mrow> \sqrt{|\mathrm{disc}_K(\mathcal{O}_K)|} \amp = |\mathcal{O}_K/I| \sqrt{|\mathrm{disc}_K(I)|}  </mrow>
    </md>
    <p>
      et la comparaison de ces formules donne le résultat.
    </p>
  </proof>

  <remark>
    Nous verrons plus loin que la norme d'un idéal est multiplicative : si <m>I,J\subset \mathcal{O}_K</m> sont des idéaux non nuls, alors <m>N(IJ)=N(I)N(J)</m>.
  </remark>

  <p>
    Il suit de la <xref ref="prop-norme-ideal-element"/> que si <m>I</m> est principal, alors <m>N(I)</m> coincide avec la norme (en valeur absolue) d'un élémént de <m>I</m>. Ce n'est pas le cas si <m>I</m> n'est pas principal, mais le théorème de Minkowski assure que l'on peut trouver un élément de <m>I</m> dont la "distance" à <m>N(I)</m> est contrôlée par le discriminant. On pose 
  </p>
  <md>
    C(r,n):=\frac{n!}{n^n}\left(\frac{4}{\pi}\right)^{r}.
  </md>
  
  <theorem xml:id="thm-minkowski">
    <title>Minkowski</title>
    <statement>
      <p>
        Soit <m>I</m> un idéal non nul de <m>\mathcal{O}_K</m>. Alors il existe <m>a\in I</m> tel que 
      </p>
      <md>
        |N_K(a)|\leq C(r_2,n)N(I)\sqrt{|\mathrm{disc}_K(A)|}.
      </md>
    </statement>
  </theorem>
  <proof>
    <p>
      On considère l'espace <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m> muni de la norme
    </p>
    <md>
      \|(x_1,\ldots,x_{r_1},z_1,\ldots,z_{r_2})\|=\sqrt{\sum_{i=1}^{r_1}{|x_i|^2}+2\sum_{j=1}^{r_2}{|z_j|^2}}.
    </md>
    <p>
      La boule <m>C_t:=\{v\in \mathbb{R}^{r_1}\times \mathbb{C}^{r_2}~|~\|v\|\leq t\}</m> de rayon <m>t</m> est un convexe symétrique et son volume se calcule par le lemme qui suit.
    </p>
    <lemma xml:id="lem-volume-boule">
      <statement>
        <p>
          Soit <m>\lambda</m> la mesure de Lebesgue sur <m>\mathbb{R}^{r_1}\times \mathbb{C}^{r_2}</m>. Alors
        </p>
        <md>
          \lambda(C_t)=2^{r_1}\left(\frac{\pi}{2}\right)^{r_2}\frac{t^n}{n!}.
        </md>
      </statement>
    </lemma>
    <p>
      Le choix de cette norme vient de l'inégalité arithmetico-géométrique<fn>qui découle de la convexité de la fonction exponentielle réelle.</fn> :
    </p>
    <md>
      \left(\prod_{i=1}^{r_1}{|x_i|}\prod_{j=1}^{r_2}{|z_j|^2}\right)^{\frac{1}{n}}\leq \frac{1}{n}\|(x_1,\ldots,x_{r_1},z_1,\ldots,z_{r_2})\|.
    </md>
    <p>
      qui entraîne <m>|N_K(x)|\leq n^{-n}\|\sigma(x)\|^n</m> pour tout <m>x\in K</m>. Enfin, on choisit <m>t</m> de sorte que <m>\lambda(C_t)=2^n\mathrm{covol}(\sigma(I))</m>. Comme <m>C_t</m> est compact, le <xref ref="lem-corps-convexe"/> du corps convexe assure que <m>C_t</m> contient un élément non nul de <m>\sigma(I)</m>. En notant <m>\sigma(a)</m> cet élément, alors on a que <m>a\in I\setminus\{0\}</m> est tel que 
    </p>
    <md>
      <mrow> |N_K(a)| \amp \leq n^{-n}t^n  </mrow> 
      <mrow> \amp = \frac{n!}{2^{r_1}\left(\frac{\pi}{2}\right)^{r_2}n^n} 2^{n}\mathrm{covol}(\sigma(I)) </mrow>
      <mrow> \amp = C(r_2,n)N(I)\sqrt{|\mathrm{disc}(\mathcal{O}_K)|}  </mrow>.
    </md>
  </proof>

  <corollary xml:id="cor-minkowski">
    <title>Minkowski</title>
    <statement>
      <p>
        Si <m>K\neq \mathbb{Q}</m>, alors <m>|\mathrm{disc}(\mathcal{O}_K)|>\frac{\pi}{3}\left(\frac{3\pi}{4}\right)^{n-1}</m>. En particulier, on a <m>\mathrm{disc}(\mathcal{O}_K)\neq \pm 1</m>. 
      </p>
    </statement>
  </corollary>
  <proof>
    <p>
      Comme tout élément non nul de <m>\mathcal{O}_K</m> est de norme <m>\geq 1</m> en valeur absolue, le <xref ref="thm-minkowski"/> donne
    </p>
    <md>
      |\mathrm{disc}(\mathcal{O}_K)|\geq C(r_2,n)^{-2}\geq C(\frac{n}{2},n)^{-2}=\frac{n^{2n}}{n!^2}\left(\frac{\pi}{4}\right)^n
    </md>
    <p>
      où la seconde inégalité découle du fait que <m>r_2\leq \frac{n}{2}</m> et <m>\pi &lt; 4</m>. 
    </p>
    <p>
    De plus, si <m>n\geq 2</m> par hypothèse. En posant <m>a_n</m> pour le terme de droite, on a <m>\frac{a_{n+1}}{a_n}=\frac{\pi}{4}\left(1+\frac{1}{n}\right)^{2n}</m> qui est une suite strictement croissante <m>\geq \frac{3\pi}{4}</m> pour <m>n\geq 2</m>. On a donc
    </p>
    <md>
      a_n\geq \left(\frac{3\pi}{4}\right)^{n-2}a_2=\frac{\pi}{3}\left(\frac{3\pi}{4}\right)^{n-1}.
    </md>
  </proof>

  <remark>
    <p>
      Ce corollaire est remarquable en bien des aspects. Par exemple, on en déduit qu'étant donné un polynôme irréductible unitaire à coefficients entiers <m>P(X)</m> de degré <m>n\geq 2</m>, il existe nécessairement un nombre premier <m>p</m> tel que <m>P(X)\pmod{p}</m> admette une racine double. 
    </p>
    <p>
      En effet, le corps <m>\mathbb{Q}[X]/(P(X))</m> s'identifie à un corps de nombre de la forme <m>K=\mathbb{Q}(\alpha)</m> où <m>\alpha</m> est une racine complexe de <m>P(X)</m>, puis 
    </p>
    <md>
      |\mathrm{disc}(P)|=|\mathrm{disc}(\mathbb{Z}[\alpha])|\geq |\mathrm{disc}(\mathcal{O}_K)|>1.
    </md>
    <p>
      Comme <m>\mathrm{disc}(P)\in \mathbb{Z}</m>, il est divisible par un nombre premier <m>p</m>. Or la réduction modulo <m>p</m> du discriminant de <m>P(X)</m>, qui est nulle, coincide  au discriminant de la réduction de <m>P(X)</m> modulo <m>p</m>.
    </p>
  </remark>

</section>



<section xml:id="sec-groupe-des-classes">
  <title>Groupe des classes d'idéaux</title>



  <p>
    On se pose maintenant la question de savoir si l'anneau <m>\mathcal{O}_K</m> est principal. Nous vérons que ce n'est pas toujours le cas, et le défaut de principalité est mésuré par un groupe abélien fini appelé le <em>groupe des classes d'idéaux</em> de <m>K</m>.
  </p>
  
  <definition xml:id="def-classe">
    <statement>
      <p>
        Soit <m>A</m> un anneau intègre et notons <m>K</m> son corps de fractions.
      </p>
      <p>
        Deux idéaux non nuls <m>I</m> et <m>J</m> de <m>A</m> sont dits <em>équivalents</em>, et on note alors <m>I\sim J</m>, s'il existe deux éléments <m>a,b\in A</m> tels que <m>aI=bJ</m>. 
      </p>
      <p>
        On désigne par <m>\mathrm{Cl}(A)</m> l'ensemble des classes d'idéaux non nuls de <m>A</m> à équivalence près. On notera par <m>[I]</m> la classe d'un idéal non nul <m>I</m>.
      </p>
    </statement>
  </definition>

  <p>
    On note <m>[A]\in \mathrm{Cl}(A)</m> la classe de l'idéal principal <m>A</m>. La pertinence de cette définition vient de la proposition suivante :
  </p>
  <proposition xml:id="prop-principal-classetrivial">
    <statement>
      <p>
        Un idéal non nul <m>I</m> est principal si, et seulement si <m>[I]=[A]</m>. En particulier, <m>A</m> est principal si, et seulement si, <m>\mathrm{Cl}(A)</m> est réduit à un élément.
      </p>
    </statement>
  </proposition>
  <proof>
    <p>
      Soit <m>I=(c)</m> un idéal principal non nul de <m>A</m>. Si <m>J\sim I</m>, alors il existe <m>a,b\in A</m> tels que <m>aI=bJ</m>, soit <m>ac=bJ</m>. Comme <m>c\neq 0</m>, on a <m>b\neq 0</m> puis <m>J=\frac{ac}{b}\cdot A</m> comme sous-ensemble de <m>K</m>. Mais comme <m>\frac{ac}{b}\cdot 1\in J\subset A</m>, on a <m>\frac{ac}{b}\in A</m> et donc <m>J</m> est l'idéal principal engendré par <m>\frac{ac}{b}</m>.
    </p>
  </proof>
  
  <p>
    Soient <m>I,J</m> deux ideaux non nuls de <m>A</m>. Rappelons que l'on écrit <m>IJ</m> pour désigner l'idéal :
  </p>
  <md>
    IJ:=\{\sum_{i=1}^s{a_i b_i}~|~a_i\in I,~b_i\in J\}.
  </md>
  <p>
    Cette opération est commutative (on a en effet <m>IJ=JI</m>) et associative (on a <m>(IJ)K=I(JK)</m>).
  </p>

  <proposition xml:id="prop-composition-classe">
    <statement>
      <p>
        Le produit des idéaux induit une loi de composition commutative et associative sur <m>\mathrm{Cl}(A)</m> dont l'élément neutre est <m>[A]</m>. 
      </p>
    </statement>
  </proposition>
  <proof>
    <p>
      Soient <m>I,J\subset A</m> des idéaux non nuls. Si <m>I'\sim I</m> et <m>J'\sim J</m>, alors il existe <m>a,b,a',b'\in A</m> tels que <m>aI=bI'</m> et <m>a'J=b'J'</m>. En multipliant ces égalités, on trouve <m>(aa')(IJ)=(bb')(I'J')</m>, soit <m>IJ\sim I'J'</m>. Par conséquent, la loi de composition induite par le produit d'idéaux sur les classes d'idéaux est bien définie. De plus, elle est commutative et associative car le produit d'idéaux l'est.
    </p>
  </proof>

  <p>
    Ce qui manque à <m>\mathrm{Cl}(A)</m> pour être un groupe est l'existence d'inverses. Remarquons qu'un idéal <m>I</m> est tel que <m>[I]</m> est inversible dans <m>\mathrm{Cl}(A)</m> si, et seulement si, il existe un idéal <m>J</m> tel que <m>IJ</m> est principal. Nous dirons alors que <m>I</m> est <em>inversible</em>.
  </p>

  <p>
    Soit <m>K</m> un corps de nombres. Nous allons montrer deux théorèmes fondamentaux : d'abord que <m>\mathrm{Cl}(\mathcal{O}_K)</m> est fini, puis que c'est un groupe. 
  </p>

  <theorem xml:id="thm-finitude-nombre-de-classe">
    <title>Finitude du nombre de classes</title>
    <statement>
      <p>
       <m>\mathrm{Cl}(\mathcal{O}_K)</m> est un ensemble fini. 
      </p>
    </statement>
  </theorem>
  <proof>
    <p>
      Soit <m>N</m> la partie entière de <m>C(r_2,n)\sqrt{|\mathrm{disc}(\mathcal{O}_K)|}</m> où <m>C(r_2,n)</m> est la constante de Minkowski (<em>cf.</em> <xref ref="thm-minkowski"/>). Nous allons montrer que tout idéal non nul de <m>\mathcal{O}_K</m> est équivalent à un idéal contenant l'un des entiers <m>\{1,\ldots,,N\}</m> et qu'il n'y a qu'un nombre fini d'idéaux de <m>\mathcal{O}_K</m> contenant un entier donné. 
    </p>
    <p>
      Soit <m>I</m> un idéal non nul de <m>\mathcal{O}_K</m>. D'après le <xref ref="thm-minkowski"/>, il existe <m>a\in I</m> tel que <m>c:=|I/(a)|\leq N</m>. Par Lagrange, <m>c\cdot I\subset (a)</m>. On a donc
    </p>
    <md>
      (c)= \frac{c}{a}(a)\subset \frac{c}{a}I \subset \mathcal{O}_K.
    </md>
    <p>
      En posant <m>J:=\frac{c}{a}I</m>, on trouve que <m>J\sim I</m> et que <m>J</m> contient l'entier <m>c\in \{1,\ldots,N\}</m>. 
    </p>
    <p>
      Il reste à montrer qu'il n'y a qu'un nombre fini d'idéaux contenant <m>c</m>. Mais ces idéaux sont en bijection avec les idéaux de <m>\mathcal{O}_K/(c)</m> qui est un anneau fini à <m>|N_K(c)|=c^n</m> éléments.
    </p>
  </proof>


  <theorem xml:id="thm-classe-groupe">
    <statement>
      <p>
        Soit <m>K</m> un corps de nombres. Alors <m>\mathrm{Cl}(\mathcal{O}_K)</m> est un groupe abélien fini. 
      </p>
    </statement>
  </theorem>
  <proof>
    <p>
      On commence par une application du lemme de Nakayama: 
    </p>
    <lemma xml:id="lem-bebe-thm-classe-groupe">
      <statement>
        <p>
          Si <m>J,J'</m> sont deux idéaux non nuls de <m>\mathcal{O}_K</m> tels que <m>JJ'=(a)J'</m> pour un certain <m>a\in \mathcal{O}_K</m>, alors <m>J=(a)</m>.
        </p>
      </statement>
    </lemma>
    <proof>
      <p>
        Montrons que <m>\frac{1}{a}J\subset \mathcal{O}_K</m>. Soit <m>j\in J</m>, de sorte que <m>\frac{j}{a}J'\subset J'</m>.On sait que <m>J'</m> est un <m>\mathbb{Z}</m>-module libre de rang fini, et si l'on fixe une base de <m>J'</m>, alors l'élément <m>\frac{j}{a}</m> agit par multiplication sur cette base comme une matrice à coefficients entiers. Par Cayley-Hamilton, le polynôme caractéristique de cette matrice annule <m>\frac{j}{a}</m>, et comme ses coefficients sont entiers, on en déduit que <m>\frac{j}{a}\in \overline{\mathbb{Z}}\cap K=\mathcal{O}_K</m>. Par conséquent, <m>J\subset (a)</m>.
      </p>
      <p>
        Soit alors <m>J'':=\frac{1}{a}J</m>. On a alors <m>J'' J'=J'</m>, et le lemme de Nakayam permet de conclure que <m>J''=\mathcal{O}_K</m>, soit <m>J=(a)</m>.
      </p>
    </proof>

    <p>
      Soit <m>I</m> un idéal non nul de <m>\mathcal{O}_K</m>. Pour montrer qu'il est inversible, il suffit de montrer qu'il existe un entier <m>n\geq 1</m> tel que <m>I^n</m> est principal (car alors la classe de <m>I^{n-1}</m> est l'inverse de la classe de <m>I</m>). Comme <m>|\mathrm{Cl}(A)|</m> est fini, il existe deux entiers <m>0 &lt; i &lt; j</m> tels que <m>I^i\sim I^j</m>. Il existe alors des éléments <m>a,b\in \mathcal{O}_K</m> tels que <m>aI^i=bI^j</m>, soit <m>aI^i=bI^j</m>, et donc <m>(bI^{j-i})I^i=(a)I^i</m>. En appliquant le <xref ref="lem-bebe-thm-classe-groupe"/>, on trouve <m>bI^{j-i}=(a)</m> puis que <m>I^{j-i}</m> est principal d'après la <xref ref="prop-principal-classetrivial"/>. 
    </p>
  </proof>
  
</section>


</chapter>